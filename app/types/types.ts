export type TeamData = {
    id: number;
    name: string;
    imageurl: string;
}

export type PlayerData = {
    id_player: number;
    name: string;
    team: string;
    position: string;
    nba_id: string;
}
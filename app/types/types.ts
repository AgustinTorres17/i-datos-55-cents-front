export type TeamData = {
    id: number;
    name: string;
    logo: string;
}

export type PlayerData = {
    id: number;
    name: string;
    team: string;
    position: string;
    image: string;
}